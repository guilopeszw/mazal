.serena
.claude

# Per-harness copies of the same locally-installed agent tooling `.claude` already
# covers. One person's editor setup, not the team's build.
.agents
.codex
.cursor
.gemini
.opencode

node_modules
.next
.turbo
dist
*.tsbuildinfo

# Raw Kaggle datasets. Olist is CC BY-NC-SA — only aggregate statistics
# get committed, in packages/data/benchmarks.json. See docs/plan/B-data.md.
data/raw/
.vercel
.env*
