.serena
.claude

# Per-harness copies of the same locally-installed agent tooling `.claude` already
# covers. One person's editor setup, not the team's build.
.agents
.codex
.cursor
.gemini
.opencode

node_modules
# pnpm's content-addressable store, if someone runs with a repo-local store dir.
.pnpm-store
.next
.turbo
dist
*.tsbuildinfo

# Raw Kaggle datasets. Olist is CC BY-NC-SA — only aggregate statistics
# get committed, in packages/data/benchmarks.json. See docs/plan/B-data.md.
data/raw/

# The audit trail is a runtime artefact of whichever machine ran it.
.mazal-audit.jsonl

# Deployment state and secrets. `.env.example` is the exception — it carries no
# values, and it is the one env file a cold clone needs to know what to set.
.vercel
.env*
!.env.example

# macOS writes these into every directory it opens. Three reached the repo once
# already.
.DS_Store
